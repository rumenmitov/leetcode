#include <vector>

using namespace std;

class Solution {
public:
    int minCostClimbingStairs(vector<int>& cost) {
      for (auto cit = cost.cbegin(); cit != cost.cend(); cit++) {
	if (cit.
      }
    }
};
